services:
  readsmith:
    build: .
    container_name: readsmith
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:/data/readsmith.db
    volumes:
      - ./data:/data
    restart: unless-stopped
